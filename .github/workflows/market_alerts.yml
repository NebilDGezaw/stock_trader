name: Market Alerts

on:
  # ── Scheduled sessions ──────────────────────────────────
  schedule:
    # LONDON SESSION: 8:00 AM UTC (Mon-Fri)
    - cron: "0 8 * * 1-5"

    # NEW YORK SESSION: 9:30 AM ET = 14:30 UTC (Mon-Fri)
    - cron: "30 14 * * 1-5"

  # ── Manual trigger (for testing) ────────────────────────
  workflow_dispatch:
    inputs:
      scan_type:
        description: "What to scan"
        required: false
        default: "all"
        type: choice
        options:
          - stocks_private
          - london_group
          - ny_group
          - all

jobs:
  # ─────────────────────────────────────────────────────────
  #  PRIVATE DM: Stocks + Leveraged ETFs (daily at NY open)
  #  Auto-selects ICT for stocks, Momentum for leveraged
  # ─────────────────────────────────────────────────────────
  stocks-private:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: >-
      github.event.schedule == '30 14 * * 1-5' ||
      github.event.inputs.scan_type == 'stocks_private' ||
      github.event.inputs.scan_type == 'all'

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
      - run: pip install yfinance pandas numpy

      - name: "🏛️ Stocks + Leveraged → Private DM"
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: >-
          python -m alerts.telegram_bot
          --tickers MSTU MSTR MSTZ TSLL SPY AAPL MSFT TSLA GOOGL NVDA META
          --stock-mode
          --always-report MSTU MSTR MSTZ TSLL
          --label "🏛️ US Stocks & Leveraged — NY Open"
          --interval 1d --period 6mo

  # ─────────────────────────────────────────────────────────
  #  GROUP: London Session
  #  Forex (auto-selects Forex ICT on 1h) + Gold & Silver
  #  (auto-selects Crypto Momentum for commodities)
  # ─────────────────────────────────────────────────────────
  london-group:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: >-
      github.event.schedule == '0 8 * * 1-5' ||
      github.event.inputs.scan_type == 'london_group' ||
      github.event.inputs.scan_type == 'all'

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
      - run: pip install yfinance pandas numpy

      - name: "🇬🇧 London Session → Group"
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_GROUP_CHAT_ID }}
        run: >-
          python -m alerts.telegram_bot
          --tickers EURUSD=X GBPUSD=X EURGBP=X GBPJPY=X USDCHF=X GC=F SI=F
          --label "🇬🇧 London Session — Forex & Metals"

  # ─────────────────────────────────────────────────────────
  #  GROUP: New York Session
  #  Crypto (auto-selects Crypto Momentum) + Gold & Silver
  #  + NY Forex (auto-selects Forex ICT on 1h)
  # ─────────────────────────────────────────────────────────
  ny-group:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: >-
      github.event.schedule == '30 14 * * 1-5' ||
      github.event.inputs.scan_type == 'ny_group' ||
      github.event.inputs.scan_type == 'all'

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
      - run: pip install yfinance pandas numpy

      - name: "🇺🇸 NY Session → Group"
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_GROUP_CHAT_ID }}
        run: >-
          python -m alerts.telegram_bot
          --tickers GC=F SI=F BTC-USD ETH-USD SOL-USD XRP-USD BNB-USD USDJPY=X USDCAD=X GBPUSD=X
          --label "🇺🇸 NY Session — Crypto, Metals & Forex"
